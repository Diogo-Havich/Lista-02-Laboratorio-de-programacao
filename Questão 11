#include <stdio.h>

int main()
  {
  int A, B, i;
  puts("Digite os valores de A e B\n");
  scanf("%d%d", &A, &B);
  i = A;
  while(i % 4 != 0)
    {
    i++;
    } 
  while(i <= B)
    {
    printf("%d\n", i * i);
    i = i + 4;
    }
  return 0;
  }
