#include <stdio.h>

int main()
  {
  int num;
  puts("Digite o numero");
  scanf("%d", &num);
  if(num / 10000 == num % 10 && (num / 1000) % 10 == ((num % 100) / 10))
    puts("O número é um palindromo");
  return 0;
  }
