#include <stdio.h>

int main()
  {
  int F, fat;
  puts("\nDigite o valor\n");
  scanf("%d", &F);
  fat = F;
  printf(" %d! = %d", F, F);
  while(F > 1)
    {
    printf(" X %d", F - 1);
    F--;
    fat = fat * F;
    }
  printf(" = %d\n", fat);
  return 0;
  }
   
