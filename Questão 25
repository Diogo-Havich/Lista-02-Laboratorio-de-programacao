#include <stdio.h>

int main() 
  {
  int sin = 1;
  float pi = 0, den = 1;
  while(1)
    {
    pi = (pi + ((4 / den) * sin));
    sin = sin *-1;
    den = den +2;
    printf("\n%f\n", pi);
    }
  return 0;
  }
  
