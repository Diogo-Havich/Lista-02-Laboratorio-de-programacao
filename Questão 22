#include <stdio.h>

int main()
  {
  int num, cont = 0;
  puts("Digite o numero");
  scanf("%d", &num);
  while(num > 1)
    {
    if(num % 10 == 7)
      {
      cont = cont + 1;
      }
    num = num / 10;
    }
  printf("O numero 7 foi digitado %d vezes\n", cont);
  return 0;
  }
