#include <stdio.h>

int main()
  {
  int i = 0, soma = 0;
  float num = 1;
  do
    {
    puts("Digite um valor diferente de 0");
    scanf("%f", &num);
    soma = soma + num;
    i++;
    }
  while(num != 0);
  printf("\nMédia = %.2f\n", (float) soma / i);
  return 0;
  }
    
