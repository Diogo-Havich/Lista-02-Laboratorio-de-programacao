#include <stdio.h>

int main()
  {
  int i = 1, fat = 1, valor = 1;
  while(i < 10)
    {
    valor = i;
    while(valor >= 1)
      {
      fat = fat * valor;
      valor--;
      }
    printf("valor %d\n", fat);
    fat = 1;
    i = i + 2;
    }
    return 0;
  }
